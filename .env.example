# Load credentials from the Tahti API (preferred in production).
TAHTI_API_BASE=https://api.tahti.live
INTERNAL_SECRET=

# Fallback if the API is unreachable. Board admins can also set these in
# Tahti Player → Settings → Add-ons → Radio.
DISCORD_CLIENT_ID=
DISCORD_TOKEN=
